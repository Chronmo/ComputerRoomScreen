{
  "name": "computer-room-screen",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-dashboard-data.mjs",
    "dev": "vite --host 0.0.0.0",
    "build": "npm run generate:data && vue-tsc -b && vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vue": "^3.5.16",
    "vue-tsc": "^2.2.10"
  },
  "devDependencies": {}
}
